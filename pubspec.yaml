name: invoice_ninja_scripts
description: CLI scripts for Invoice Ninja (uses package invoice_ninja_client).
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

executables:
  ninja: ninja

environment:
  sdk: ^3.10.1

# Add regular dependencies here.
dependencies:
  args: ^2.5.0
  cli_completion: ^0.5.1
  diacritic: ^0.1.6
  ffi: ^2.1.0
  invoice_ninja_client:
    path: packages/invoice_ninja_client
  mason_logger: ^0.3.5
  path: ^1.9.0
  win32: ^5.15.0

dev_dependencies:
  test: ^1.25.6
  very_good_analysis: ^10.1.0
