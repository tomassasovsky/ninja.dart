name: invoice_ninja_client
description: HTTP client for the Invoice Ninja REST API (v1).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.1

dependencies:
  http: ^1.2.0

dev_dependencies:
  mason_logger: ^0.3.5
  test: ^1.25.6
  very_good_analysis: ^10.1.0
